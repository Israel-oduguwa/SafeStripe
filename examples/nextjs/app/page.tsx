export default function Page() {
  return (
    <main>
      <h1>SafeStripe sandbox</h1>
      <p>
        Use the authenticated checkout endpoint described in the quickstart. Payment status is
        updated by the webhook worker.
      </p>
    </main>
  );
}
